/*
 * BlockMenu Messages
 *
 * This contains all the text for the BlockMenu container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BlockMenu';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BlockMenu container!',
  },
});
