/*
 * CredentialsField Messages
 *
 * This contains all the text for the CredentialsField component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CredentialsField';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CredentialsField component!',
  },
});
