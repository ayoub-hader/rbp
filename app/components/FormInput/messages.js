import { defineMessages } from 'react-intl';

export const scope = 'app.components.FormInput';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FormInput component!',
  },
});
